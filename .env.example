# ===========================================
# cp .env.example .env
# БД не в Docker — укажи свои Postgres (локально или другая VM)
# ===========================================

# БД mentors (дашборд) — для авторизации
# Локально: postgresql://user:password@localhost/test
# Docker с хоста: postgresql://user:password@host.docker.internal:5432/test
# С другой VM: postgresql://user:password@ip_или_хост:5432/test
AUTH_DATABASE_URL=

# БД coution — страницы и блоки
# Локально: postgresql://user:password@localhost/coution
# Docker: postgresql://user:password@host.docker.internal:5432/coution
# Другая VM: postgresql://user:password@ip_или_хост:5432/coution
COUTION_DATABASE_URL=

# JWT секрет. Минимум 32 символа
JWT_SECRET=

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
