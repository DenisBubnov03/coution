# ===========================================
# Скопируй в .env и заполни своими данными
# cp .env.example .env
# ===========================================

# БД mentors (дашборд) — для авторизации
# Формат: postgresql://user:password@host:port/dbname
# В Docker: host.docker.internal вместо localhost
AUTH_DATABASE_URL=

# БД coution — для Docker заполняет compose автоматически
# Локально: postgresql://user:password@localhost/coution
COUTION_DATABASE_URL=

# JWT секрет (тот же что в дашборде). Минимум 32 символа
JWT_SECRET=

# Для Docker: пароль Postgres (coution)
POSTGRES_USER=coution
POSTGRES_PASSWORD=
POSTGRES_DB=coution

# CORS (разрешённые origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
